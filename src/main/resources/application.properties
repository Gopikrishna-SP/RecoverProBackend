spring.application.name=chatbot

# -------------------------------
# Server
# -------------------------------
server.port=8080

# -------------------------------
# DATASOURCE (Railway PUBLIC URL)
# -------------------------------
spring.datasource.url=jdbc:postgresql://mainline.proxy.rlwy.net:53170/railway
spring.datasource.username=postgres
spring.datasource.password=EjdkmIubkcffYwjNBBqJhFJZkzdojyJj
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP Connection Pool Settings
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2

# -------------------------------
# JPA / HIBERNATE (IMPORTANT)
# -------------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# CRITICAL: Use Asia/Kolkata (modern name) instead of Asia/Calcutta (deprecated)
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Kolkata

# Additional Hibernate settings
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# -------------------------------
# SECURITY
# -------------------------------
spring.security.user.name=chat
spring.security.user.password=1234

# -------------------------------
# JWT
# -------------------------------
spring.app.jwtSecret=s1lU3vHhDTk4o2QpF2CLL8UeT6nWAcZ9G2MxVYbZ3Ms=
spring.app.jwtExpirationMs=86400000

# -------------------------------
# Multipart
# -------------------------------
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

# -------------------------------
# Mail (Gmail SMTP)
# -------------------------------
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=krish.offl36@gmail.com
spring.mail.password=ysuvtcvxnuyexvoh
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

app.mail.from=krish.offl36@gmail.com
app.mail.from-name=RecoverPro